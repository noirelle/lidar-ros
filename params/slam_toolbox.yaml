slam_toolbox:
  ros__parameters:

    # ROS Parameters
    odom_frame: odom
    base_frame: base_link
    scan_topic: /scan
    use_map_saver: true
    mode: mapping # or localization

    # SLAM Parameters
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02 # 50Hz
    map_update_interval: 2.0 # Decreased frequency to allow better ray-trace clearing
    resolution: 0.05
    max_laser_range: 12.0
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000 # deactivating `preallocate_stack`
    enable_interactive_mode: true

    # Scan Matcher Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.3 # 30cm threshold to avoid 'baking in' transient objects
    minimum_travel_heading: 0.3  # ~17 degree threshold
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_maximum_distance: 2.0
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_co_var: 0.01
    loop_match_minimum_response_co_var: 0.075
    lattice_search_offset: 3
    lattice_search_rotation_interval: 0.1
    telemetry_interval: 1.0

    # Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Toolkit Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 0.5

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
